export const getUserById = () => Promise.resolve({});

// Subir publicaciones del usuario a firestore
export const loadPublications = jest.fn(() => {});
export const actualizarPosts = jest.fn(() => {});
// () => Promise.resolve({});
